# Project References

This file centralizes external reference projects used by agents and developers.
If a local checkout lives elsewhere, create or update `REFERENCES.local.md` with
the same reference name and your local path. `REFERENCES.local.md` is ignored by
git and should not be committed.

## ecs-survivors

Purpose: lifecycle, state machine, DI, scene loading, UI flow, dynamic window
flow, and gameplay loop reference.

Default local path:

```text
F:\unity_personal\Tutorials\ecs-survivors-viewers-main\src\ecs-survivors
```

Local override example:

```text
ecs-survivors: D:\work\ecs-survivors-viewers-main\src\ecs-survivors
```

